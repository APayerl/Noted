<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search"
        android:icon="@drawable/ic_search_black_24"
        android:title="Search title"
        android:contentDescription="Search description"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/more"
        android:title="More title"
        android:contentDescription="More description"
        app:showAsAction="never" />

    <item
        android:id="@+id/about"
        android:title="@string/about"
        android:contentDescription="@string/about_app"
        app:showAsAction="never" />
</menu>